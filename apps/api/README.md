# GlycemicGPT API

FastAPI backend for GlycemicGPT diabetes management platform.

## Endpoints

- `GET /health` - Health check
- `GET /health/live` - Kubernetes liveness probe
- `GET /health/ready` - Kubernetes readiness probe
